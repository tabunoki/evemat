DROP TABLE IF EXISTS PRODUCT_BLUEPRINT;

CREATE TABLE PRODUCT_BLUEPRINT (
	ID INT PRIMARY KEY AUTO_INCREMENT,
	BLUEPRINT_TYPE_ID INT,
	ME INT,
	PE INT,
	PRODUCT_GROUP INT,
);

ALTER TABLE PUBLIC.PRODUCT_BLUEPRINT ADD FOREIGN KEY (PRODUCT_GROUP) REFERENCES PUBLIC.PRODUCT_GROUP (ID);
